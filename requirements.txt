# Full dependency set for the REAPER MCP server.
#
# `scripts/bootstrap.py` installs this into a virtualenv the plugin owns, and
# hashes the file to decide whether a reinstall is needed, so editing it is what
# triggers an update on the next bootstrap.

-r requirements-core.txt

# Everything below is imported lazily, inside the functions that use it, and
# powers only the offline analysis tools - loudness, spectrum, transients,
# stereo field, clipping.
#
# It is also most of the install: librosa drags in llvmlite (117 MB), scipy
# (116 MB), scikit-learn (45 MB) and numba (30 MB). Measured, this block is
# 345 MB of the 477 MB total and 6,865 of the 12,212 files.
#
# `bootstrap.py --core` skips this block. The server starts and every structural
# tool works; the analysis tools report that they need the full install. Worth
# it on a disposable VM or a metered connection.
librosa>=0.10.0
pyloudnorm>=0.1.1
soundfile>=0.12.0
