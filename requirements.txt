# Dependencies for the REAPER MCP server.
#
# Modifications to this file will trigger a reinstall during the next bootstrap
# process, as scripts/bootstrap.py hashes this file to track changes.
#
# The mcp dependency is pinned below 2.0 to maintain compatibility with the
# mcp.server.fastmcp API, which was removed in the 2.x release.
mcp>=1.0.0,<2.0.0

python-reapy>=0.10.0
numpy>=1.24.0

# The following dependencies are required for offline analysis tools (loudness,
# spectrum, transients, stereo field, clipping). They are imported lazily to
# minimize server startup time.
#
# librosa transitively installs llvmlite, scipy, scikit-learn, and numba.
librosa>=0.10.0
pyloudnorm>=0.1.1
soundfile>=0.12.0
